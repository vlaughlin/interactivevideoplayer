$('#video').mediaelementplayer({
  stretching: 'responsive',
  features: ['playpause', 'current', 'progress', 'duration', 'volume', 'fullscreen'],
});

//Define variables//

var videoElement = document.getElementById('video');
var transcriptElement = document.getElementById('transcript');
var captionElement = document.querySelectorAll('#transcript span');


// Highlight //

$('#video').on('timeupdate', () => {
        for (var i = 0; i < captionElement.length; ++i) {
                let time = videoElement.currentTime;
                let start = captionElement[i].getAttribute(
                        'data-start');
                let end = captionElement[i].getAttribute(
                        'data-end');
                if (time > start && time < end) {
                        captionElement[i].className =
                                'highlight';
                } else {
                        captionElement[i].className = "";
                }
        }
});

// Click //

transcriptElement.addEventListener('click', (event) => {
        if (event.target.tagName === 'SPAN') {
                videoElement.currentTime = event.target.getAttribute(
                        'data-start');
        }
});
